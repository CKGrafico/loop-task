## 1. Project setup: dependencies, config, CI

- [x] 1.1 Add `playwright` and `@playwright/test` as devDependencies, plus `test:e2e`, `test:e2e:gen`, and per-suite shims (`test:e2e:loops`, `:tasks`, `:projects`, `:commands`, `:modals`, `:shortcuts`, `:breakpoints`) to package.json scripts. Document ttyd as a dev prereq in README "Testing the board in a browser" section. <!-- agent: basic-engineer.fast, depends_on: [], touches: [package.json, README.md] -->
- [x] 1.2 Create `tests/e2e/playwright.config.ts` with Chromium project, `webServer` hook owning the ttyd+board lifecycle, retries/headless, trace `retain-on-failure`, and the `LOOP_CLI_HOME` env injection. Add `tests/e2e/.gitignore` covering `.runs/`. <!-- agent: basic-engineer.build, depends_on: [1.1], touches: [tests/e2e/playwright.config.ts, tests/e2e/.gitignore] -->
- [x] 1.3 Add `.github/workflows/e2e.yml`: matrix Windows-2022 + ubuntu-latest, install ttyd (`winget`/`apt`), `pnpm exec playwright install --with-deps chromium`, run `pnpm test:e2e`, upload `tests/e2e/.runs/` on failure. <!-- agent: basic-engineer.build, depends_on: [1.1, 1.2], touches: [.github/workflows/e2e.yml] -->

## 2. Harness: fixtures, driver, helpers

- [x] 2.1 `tests/e2e/fixtures/board.ts`, global Playwright fixture: pick ephemeral temp `LOOP_CLI_HOME` under OS temp, pick ephemeral ttyd port, fail fast if `ttyd` missing (print install hints), spawn `ttyd -W -w <repo> -p <port> pnpm run dev` with `LOOP_CLI_HOME` in env, poll `http://127.0.0.1:<port>` until `.xterm` exists (50ms/10s), afterAll kill processes + clean temp dir. <!-- agent: basic-engineer.build, depends_on: [1.2], touches: [tests/e2e/fixtures/board.ts] -->
- [x] 2.2 `tests/e2e/fixtures/terminal-page.ts`, `TerminalPage` driver: focus `.xterm-helper-textarea`, wrap `page.keyboard` for printable type / Enter / Escape / Backspace / Arrow keys / Tab / Shift+Tab / Ctrl hold-press-release chords / multi-line paste via dispatched `paste` event. Never set `.value` directly. Provide `snapshot()` (PNG to trace dir) and `readText()` (xterm.js buffer introspection via `page.evaluate`, OCR fallback). Provide `waitUntilReady()` polling xterm canvas length > 0. <!-- agent: basic-engineer.build, depends_on: [2.1], touches: [tests/e2e/fixtures/terminal-page.ts] -->
- [x] 2.3 `tests/e2e/fixtures/data.ts`, seed/reset helpers writing `loops.json`/`tasks.json`/`projects.json` into the temp `LOOP_CLI_HOME` atomically so suites start from a known state without going through the board UI. <!-- agent: basic-engineer.build, depends_on: [2.1], touches: [tests/e2e/fixtures/data.ts] -->
- [x] 2.4 `tests/e2e/helpers/bugs.ts`, `recordBug({area,symptom,steps,expected,actual})` that appends a numbered entry to `tests/e2e/bugs.md` (atomic write) and returns a `fixme(test, body)` wrapper that wraps Playwright `test.fixme` while still asserting the body so a fixed bug flips to fail. <!-- agent: basic-engineer.fast, depends_on: [1.2], touches: [tests/e2e/helpers/bugs.ts] -->
- [x] 2.5 `tests/e2e/helpers/assertions.ts`, `expectTerminalText(term, substring)`, `expectVisible(term, regex)`, `expectToast(term, substring)` that read from `TerminalPage.readText()` with sensible settle polls (default 50ms × 200 retries = 10s) before asserting. <!-- agent: basic-engineer.fast, depends_on: [2.2], touches: [tests/e2e/helpers/assertions.ts] -->

## 3. Board E2E suites

- [x] 3.1 `tests/e2e/suites/loops-crud.spec.ts`, scenarios from `board-e2e-suites` spec: empty-state welcome, create via `n`, edit via `e` (pauses), pause/resume `p`, stop/play `s` (schedule reset), trigger `t`, clone via `clone` command, delete via `d` + `yes`, open run-log modal `o`/`logs` (search + copy + close). Use `data.ts` to seed one loop for lifecycle tests. <!-- agent: frontend-engineer.build, depends_on: [2.1,2.2,2.3,2.5], touches: [tests/e2e/suites/loops-crud.spec.ts] -->
- [x] 3.2 `tests/e2e/suites/tasks-crud.spec.ts`, switch to Tasks via `2`/Ctrl+→, new task via `n` with on-success chain to existing task (seed first task via `data.ts`), edit via `e`, delete via `d` + `yes`, inspector reflects name/command/chain summary. <!-- agent: frontend-engineer.build, depends_on: [2.1,2.2,2.3,2.5], touches: [tests/e2e/suites/tasks-crud.spec.ts] -->
- [x] 3.3 `tests/e2e/suites/projects-crud.spec.ts`, switch via `3`, create project via `n` + color, edit via `e` (non-system only), confirm Default is non-editable/non-deletable, delete moves loops to Default, cycle has-loops `r` / is-system `t` / sort `o` filters. <!-- agent: frontend-engineer.build, depends_on: [2.1,2.2,2.3,2.5], touches: [tests/e2e/suites/projects-crud.spec.ts] -->
- [x] 3.4 `tests/e2e/suites/command-bar.spec.ts`, exercise every entry in `commandHandlers` (edit, clone, delete, pause, play, stop, trigger, new-loop, new-task, new-project, project-filter-loops/type, project-sort, all-commands, help, search, filter-status, sort, filter-project, debug, logs, select, api, status, export, import) verifying each dispatches with no "Unknown command" toast; unknown command produces "Unknown command: xyz" toast; fuzzy/prefix dropdown match dispatches on Enter. <!-- agent: frontend-engineer.build, depends_on: [2.1,2.2,2.5], touches: [tests/e2e/suites/command-bar.spec.ts] -->
- [x] 3.5 `tests/e2e/suites/modals.spec.ts`, `help`/`all-commands` browser (navigate, Enter dispatches, Esc closes), run-log modal (open `/` search `c` copy Esc close), export modal Ctrl+X (JSON preview + saved path + `c` copy), context-help modal open/close, API toast via Ctrl+G, confirm dialog yes/cancel paths. <!-- agent: frontend-engineer.build, depends_on: [2.1,2.2,2.5], touches: [tests/e2e/suites/modals.spec.ts] -->
- [x] 3.6 `tests/e2e/suites/keyboard-shortcuts.spec.ts`, 1/2/3 tab switches, Tab/Shift+Tab panel swap, ←/→ panel focus, Ctrl+←/→ cycle tabs, Ctrl+A then action letter (n/e/d/p/r/s/t/c/o), Ctrl+F then filter letter (s/t/o/p), Ctrl+B debug, Ctrl+G api, Ctrl+X export, Ctrl+I import, Ctrl+Y status, Ctrl+Enter contextual (left=edit, right=open log). <!-- agent: frontend-engineer.build, depends_on: [2.1,2.2,2.5], touches: [tests/e2e/suites/keyboard-shortcuts.spec.ts] -->
- [x] 3.7 `tests/e2e/suites/breakpoints.spec.ts`, wide (120 cols): side-by-side + full header; narrow (70 cols): stacked panels + compact header; Tab still swaps focus, command bar still receives keys in narrow mode. <!-- agent: frontend-engineer.build, depends_on: [2.1,2.2,2.5], touches: [tests/e2e/suites/breakpoints.spec.ts] -->

## 4. Generated artifacts

- [x] 4.1 Create `tests/e2e/bugs.md` skeleton (header + status convention + how `recordBug` entries are formatted) so suite runs can append. Initial example entry demonstrates the format. <!-- agent: basic-engineer.fast, depends_on: [2.4], touches: [tests/e2e/bugs.md] -->
- [x] 4.2 Create `tests/e2e/generate-manual.ts` that walks every Playwright suite, extracts each `test.info('…')` block, and writes `tests/e2e/MANUAL-TEST.md` organised by suite → step. Wire it as the `test:e2e:gen` script and run once to produce the initial manual. <!-- agent: basic-engineer.fast, depends_on: [1.1,3.1,3.2,3.3,3.4,3.5,3.6,3.7], touches: [tests/e2e/generate-manual.ts, tests/e2e/MANUAL-TEST.md] -->

## 5. Verification

- [x] 5.1 Run `rtk pnpm typecheck`, `rtk pnpm lint`, and `rtk pnpm test` from repo root to confirm the new `tests/e2e/` files type-clean and existing Vitest suites still pass (no broken imports, no leaked runtime deps in `src/`). Fix any type/lint errors introduced by the harness. <!-- agent: basic-engineer.fast, depends_on: [3.1,3.2,3.3,3.4,3.5,3.6,3.7,4.1,4.2], touches: [] -->